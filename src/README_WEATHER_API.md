# 🌤️ TMD Weather API Integration Guide

เอกสารสำหรับการเชื่อมต่อระบบพยากรณ์อากาศจากกรมอุตุนิยมวิทยา (TMD)

---

## 📋 สารบัญ

1. [ภาพรวม](#ภาพรวม)
2. [การติดตั้ง](#การติดตั้ง)
3. [การตั้งค่าพิกัด](#การตั้งค่าพิกัด)
4. [การใช้งาน](#การใช้งาน)
5. [ข้อมูลที่ดึงจาก API](#ข้อมูลที่ดึงจาก-api)
6. [การ Troubleshooting](#การ-troubleshooting)

---

## 🎯 ภาพรวม

ระบบนี้จะดึงข้อมูลพยากรณ์อากาศแบบเรียลไทม์จาก TMD API และแสดงผลใน Weather Widget โดย:

- ✅ **อัพเดทอัตโนมัติทุก 30 นาที**
- ✅ **แสดงข้อมูลจริงจาก TMD** (อุณหภูมิ, ลม, ความชื้น, ฯลฯ)
- ✅ **ระบบ Fallback** เมื่อ API ล้มเหลว
- ✅ **Support Dark/Light Theme**

---

## 📦 การติดตั้ง

### 1. นำไฟล์เข้าโปรเจค

คัดลอกไฟล์ 2 ไฟล์นี้ไปยังโปรเจคของคุณ:

```
src/
├── utils/
│   └── weatherService.js          # ← Service สำหรับดึงข้อมูลจาก TMD API
└── components/
    └── Overview.jsx                # ← Component ที่อัพเดทแล้ว
```

### 2. Import React

เนื่องจากใช้ React Hooks ต้องแน่ใจว่ามี React ในโปรเจค:

```bash
npm install react
# หรือ
yarn add react
```

---

## ⚙️ การตั้งค่าพิกัด

### 🔴 **สำคัญมาก! ต้องแก้พิกัดก่อนใช้งาน**

เปิดไฟล์ `weatherService.js` และแก้ไขพิกัดในส่วน **WEATHER_CONFIG**:

```javascript
// ====================================================================
// 🔧 CONFIGURATION - แก้พิกัดตรงนี้
// ====================================================================
const WEATHER_CONFIG = {
  // พิกัดโรงงาน: 13°28'26.1"N 101°05'22.3"E
  LATITUDE: 13.473917,   // ⭐ แก้พิกัด Latitude ตรงนี้
  LONGITUDE: 101.089528, // ⭐ แก้พิกัด Longitude ตรงนี้
  
  // ... ส่วนอื่นๆ
};
```

### วิธีหาพิกัดของคุณ:

1. **จาก Google Maps**:
   - คลิกขวาที่ตำแหน่งบนแผนที่
   - เลือก "What's here?" 
   - คัดลอกพิกัดที่แสดง (เช่น `13.473917, 101.089528`)

2. **แปลงพิกัด DMS → Decimal**:
   - หากพิกัดอยู่ในรูป `13°28'26.1"N` ใช้เครื่องมือแปลง:
     - https://www.fcc.gov/media/radio/dms-decimal
   - ตัวอย่าง:
     ```
     13°28'26.1"N = 13.473917°
     101°05'22.3"E = 101.089528°
     ```

### ตัวอย่างพิกัดในประเทศไทย:

| สถานที่ | Latitude | Longitude |
|---------|----------|-----------|
| **กรุงเทพฯ** | 13.7563 | 100.5018 |
| **เชียงใหม่** | 18.7883 | 98.9853 |
| **ภูเก็ต** | 7.8804 | 98.3923 |
| **นครปฐม** | 13.8199 | 100.0371 |
| **สมุทรปราการ** | 13.5991 | 100.5998 |

---

## 🚀 การใช้งาน

### 1. ใน Component หลัก (App.jsx)

**ก่อนหน้า (ใช้ Mock Data):**
```jsx
import Overview from './components/Overview';

function App() {
  // Mock weather data
  const weatherData = {
    temp: 32.5,
    condition: 'Partly Cloudy',
    // ...
  };

  return (
    <Overview 
      weatherData={weatherData}  // ❌ ส่ง Mock Data
      // ... props อื่นๆ
    />
  );
}
```

**หลังอัพเดท (ใช้ TMD API):**
```jsx
import Overview from './components/Overview_Updated'; // ใช้ไฟล์ใหม่

function App() {
  // ❌ ไม่ต้องส่ง weatherData แล้ว! 
  // Component จะดึงข้อมูลจาก API เอง

  return (
    <Overview 
      // weatherData={weatherData}  // ❌ ลบบรรทัดนี้ออก
      theme={theme}
      currentLang={currentLang}
      kpiCards={kpiCards}
      // ... props อื่นๆ
    />
  );
}
```

### 2. Component จะทำงานอัตโนมัติ

- ✅ ดึงข้อมูลจาก TMD API ครั้งแรกเมื่อโหลด
- ✅ อัพเดทอัตโนมัติทุก 30 นาที
- ✅ แสดงเวลาอัพเดทล่าสุด
- ✅ มีปุ่ม Refresh เพื่อบังคับอัพเดท

---

## 📊 ข้อมูลที่ดึงจาก API

### ข้อมูลจริงจาก TMD:

| ข้อมูล | ชื่อตัวแปร | หน่วย | API Field |
|--------|------------|-------|-----------|
| **อุณหภูมิ** | `temp` | °C | `tc` |
| **สภาพอากาศ** | `condition` | - | `cond` |
| **ความเร็วลม** | `windSpeed` | km/h | `ws10m` |
| **ทิศทางลม** | `windDirection` | degree | `wd10m` |
| **ความชื้น** | `humidity` | % | `rh` |
| **ความกดอากาศ** | `pressure` | hPa | `slp` |
| **ปริมาณฝน** | `rain` | mm | `rain` |

### ข้อมูล Mock (ไม่มีใน TMD API):

| ข้อมูล | ค่าที่ใช้ | หมายเหตุ |
|--------|---------|----------|
| **PM 2.5** | 12 µg | Mock data - มีเครื่องหมาย * |
| **AQI** | Good | Mock data - มีเครื่องหมาย * |
| **Light** | 850 W/m² | ต้องดึงจาก Daily API แยก |

---

## 🎨 UI Features

### 1. Weather Widget Header

```
🌤️ 32.5° Partly Cloudy
   └── แสดงอุณหภูมิและสภาพอากาศปัจจุบัน
   🕐 อัพเดท: 14:30
   └── แสดงเวลาอัพเดทล่าสุด
   🔄 [ปุ่ม Refresh]
   └── กดเพื่อบังคับอัพเดทข้อมูล
```

### 2. Weather Details Grid

แสดง 6 ค่าย่อย:
- 💨 Wind
- 💧 Humidity  
- 🔽 Pressure
- ☀️ Light
- PM PM 2.5 * (Mock)
- 🍃 AQI * (Mock)

### 3. Status Indicators

- ⚪ **Loading**: แสดง "..." เมื่อกำลังโหลด
- 🔴 **Error**: แสดงข้อความแจ้งเตือนสีแดง
- 🟡 **Fallback**: แสดงแบนเนอร์สีเหลืองเมื่อใช้ข้อมูลสำรอง
- 🔄 **Refreshing**: ไอคอน Refresh หมุนเมื่อกำลังดึงข้อมูล

---

## 🔧 การ Troubleshooting

### ❌ ปัญหา: "ไม่สามารถโหลดข้อมูลอากาศได้"

**สาเหตุที่เป็นไปได้:**

1. **API Token หมดอายุ**
   - ตรวจสอบที่: `WEATHER_CONFIG.API_TOKEN`
   - วิธีแก้: ขอ Token ใหม่จาก TMD

2. **พิกัดไม่ถูกต้อง**
   - ตรวจสอบ `LATITUDE` และ `LONGITUDE`
   - ต้องเป็นตัวเลข Decimal (ไม่ใช่ DMS)

3. **Network Error**
   - เช็ค Console: `F12` → Console Tab
   - ดูข้อความ Error จาก API

### ✅ วิธีเช็ค

เปิด Browser Console (`F12`) และดูข้อความ:

```
✅ กรณีสำเร็จ:
🌤️ Initializing Weather Service...
📍 Location: 13.473917°N, 101.089528°E
🔄 Fetching weather data from TMD API...
✅ Weather data updated successfully

❌ กรณีผิดพลาด:
🌤️ Initializing Weather Service...
❌ TMD API Error: 401 Unauthorized
⚠️ Using fallback weather data
```

### 🔍 Debug Mode

เพิ่มในไฟล์ `weatherService.js`:

```javascript
// หลังบรรทัด export const weatherManager = new WeatherDataManager();
if (typeof window !== 'undefined') {
  window.debugWeather = () => {
    console.log('🔍 Current Config:', getWeatherConfig());
    console.log('🔍 Current Data:', weatherManager.getCurrentData());
  };
}
```

จากนั้นใน Console พิมพ์:
```javascript
debugWeather()
```

---

## 🆘 ติดต่อ Support

หากพบปัญหาที่แก้ไม่ได้:

1. เก็บ Error Message จาก Console
2. เช็คว่าพิกัดถูกต้องหรือไม่
3. ทดสอบ API ด้วย Postman/Curl:

```bash
curl -X GET "https://data.tmd.go.th/nwpapi/v1/forecast/location/hourly/at?lat=13.473917&lon=101.089528&fields=tc,rh,slp,ws10m&duration=1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Accept: application/json"
```

---

## 📝 Checklist การติดตั้ง

- [ ] ✅ คัดลอกไฟล์ `weatherService.js` และ `Overview_Updated.jsx`
- [ ] ✅ แก้พิกัด `LATITUDE` และ `LONGITUDE` ใน `weatherService.js`
- [ ] ✅ อัพเดท Import ในไฟล์หลัก (ใช้ `Overview_Updated.jsx`)
- [ ] ✅ ลบ prop `weatherData` ออกจาก `<Overview />`
- [ ] ✅ ทดสอบใน Browser → เปิด Console ดู Log
- [ ] ✅ เช็คว่าข้อมูลแสดงถูกต้อง

---

## 🎉 สำเร็จแล้ว!

ตอนนี้ระบบของคุณจะแสดงข้อมูลพยากรณ์อากาศแบบเรียลไทม์จาก TMD แล้ว! 🌤️

**Happy Coding! 🚀**
